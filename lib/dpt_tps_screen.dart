import 'package:flutter/material.dart';

class DptTpsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('DPT Per TPS'),
      ),
      body: Center(
        child: Text('Halaman DPT Per TPS'),
      ),
    );
  }
}
