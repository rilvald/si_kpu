import 'package:flutter/material.dart';

class DptTpsScreen extends StatelessWidget {
  const DptTpsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DPT Per TPS'),
      ),
      body: const Center(
        child: Text('Halaman DPT Per TPS'),
      ),
    );
  }
}
