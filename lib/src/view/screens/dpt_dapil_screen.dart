import 'package:flutter/material.dart';

class DptDapilScreen extends StatelessWidget {
  const DptDapilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DPT Per Dapil'),
      ),
      body: const Center(
        child: Text('Halaman DPT Per Dapil'),
      ),
    );
  }
}
