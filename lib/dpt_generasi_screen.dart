import 'package:flutter/material.dart';

class DptGenerasiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('DPT Per Generasi'),
      ),
      body: Center(
        child: Text('Halaman DPT Per Generasi'),
      ),
    );
  }
}
